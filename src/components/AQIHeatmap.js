import React from "react";

export default function AQIHeatmap({ city }) {
  // Placeholder: integrate react-leaflet + heatmap plugin later with real geo data
  return (
    <div className="bg-white rounded shadow p-4 h-64 flex flex-col items-center justify-center">
      <div className="text-lg font-medium mb-2">Heatmap (placeholder)</div>
      <div className="text-sm text-gray-500">Integrate react-leaflet and heatmap layer later</div>
    </div>
  );
}
/*rudra save*/